apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: maas-gateway-policies

resources:
# Policy folders (upstream structure)
- auth-policies
- usage-policies
# RBAC for tier ServiceAccounts (required for model access authorization)
- tier-rbac.yaml
